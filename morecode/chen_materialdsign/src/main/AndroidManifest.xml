<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chenxh.recyclerview.chen_materialdsign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".demo.BottomSheetsActivity"
            android:theme="@style/MaterialAppTheme">

        </activity>
        <activity android:name=".demo.ChipActivity"   android:theme="@style/MaterialAppTheme" />
        <activity
            android:name=".demo.MaterialTextActivity"
            android:theme="@style/MaterialAppTheme" />
        <activity
            android:name=".demo.DrawerActivity"
            android:theme="@style/TranslucentTheme" />
        <activity android:name=".demo.MaterialButtonActivity" />
        <activity
            android:name=".demo.ToolBarActivity"
            android:theme="@style/ToolBarAppTheme" />
        <activity android:name=".demo.CoordinatorActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>